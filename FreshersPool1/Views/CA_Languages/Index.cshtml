@model IEnumerable<FreshersPool1.Models.CA_Languages>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            CA_PersonalData
        </th>
        <th>
            MS_LanguageMaster
        </th>
        <th>
            Language
        </th>
        <th>
            Speak
        </th>
        <th>
            Reads
        </th>
        <th>
            Write
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CA_PersonalData.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MS_LanguageMaster.Language)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Language)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Speak)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reads)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Write)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
